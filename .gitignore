target2/
target/
src-tauri/target/

# Dependencies
node_modules/

# Build output
dist/

# State engine build artifacts and runtime data
state/target/
state/data/saves/
state/data/telemetry/
state/data_backup/
state/*.txt
state/build*.txt
state/test*.txt

# Old state repo backup (de-submoduleed)
state_git_backup/

# Release commit message scratch files
commit_msg.txt
release_commit.txt

# Build log files
npm_build.txt
*.build_log.txt

# Root-level temp/test output files
build*.txt
cicd_*.txt
test*.txt
.commit_msg_*.txt
commit_v*.txt

# Devin workflow state files (ephemeral, not committed)
.devin/.plan_submitted
.devin/.clearance_granted
.devin/.cicd_state
.devin/.cicd_bypass

# Manager RBAC auth + audit (gitignored, never committed)
.devin/.manager_auth
.devin/.manager_audit_log

# Daemon v2.0 CI/CD failure state (ephemeral, not committed)
.devin/.cicd_failure_state.json
