Eradicate manual AGENTS_SYNC.md, implement cross-agent blocker bus

- Delete AGENTS_SYNC.md: manual markdown ledger eradicated. All
  inter-agent sync is now strictly via agents_sync.json + hooks.
- Expand agents_sync.json schema: add top-level "cross_agent_blockers"
  array with from_agent, to_agent, affected_file, message, timestamp.
  Pre-populated with 2 existing blockers (agent-2 and agent-3 to agent-1).
- sync_lib.sh: add get_blockers_for_agent (parses blockers targeting
  this agent), mutator_add_blocker (upsert blocker via transactional
  loop), mutator_clear_my_blockers (clear on session end).
- start.sh: parse cross_agent_blockers on SessionStart and inject
  high-visibility warnings into additionalContext. Cleanup trap now
  also clears this agent's posted blockers.
- block.sh: new CLI helper for agents to post blockers safely:
  bash .devin/scripts/block.sh <to_agent|all> <file> "<message>"
- AGENTS.md: updated to state JSON-only workflow, document blocker bus
  and block.sh usage.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
